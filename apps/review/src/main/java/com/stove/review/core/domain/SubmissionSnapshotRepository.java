package com.stove.review.core.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SubmissionSnapshotRepository extends JpaRepository<SubmissionSnapshot, Long> {
}
