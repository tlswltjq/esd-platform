.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
out/
bin/

.idea/
*.iml
*.ipr
*.iws
.vscode/
.claude/settings.local.json
.DS_Store

*.log
logs/
.env

# 측정 결과. 회차마다 CSV 가 수천 줄씩 쌓이고, 값이 있는 것은 원본이 아니라
# 그걸 읽고 문서에 남긴 판정이다 — docs/performance.md · docs/perf-tuning.md.
perf-results/

# 장애 회차 산출물 — scripts/chaos/run-scenario.sh 의 기본 --out 자리
runs/

# 에이전트 작업 공간 — 워크트리와 세션 설정. 리포에 담을 것이 아니다.
.claude/
